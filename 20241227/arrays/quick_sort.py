def main(values):

    def _sort_subarray(start_index, end_index):
        pivot = end_index - 1
        index_left = start_index - 1
        for index_right in range(start_index, pivot):
            



    _sort_subarray(0, len(values) - 1)
    return values